describe("the main UI features", () => {
  it.todo("Renders two tables for the users rankings");
});

describe("the Users Fantasy Rankings", () => {
  it.todo("Renders two tables for the users rankings");
  it.todo("only shows fantasy armies the user owns");
  it.todo("the fantasy rankings show top rank first");
});

describe("the Users 40k Rankings", () => {
  it.todo("Renders two tables for the users rankings");
  it.todo("only shows 40k armies the user owns");
  it.todo("the fantasy 40k show top rank first");
});
